{
  "name": "trabalho-js",
  "version": "1.0.0",
  "description": "Servico de pagamento com testes em Mocha e Node Assert.",
  "main": "src/ServicoDePagamento.js",
  "scripts": {
    "lint": "node --check src/ServicoDePagamento.js && node --check test/ServicoDePagamento.test.js && node --check scripts/gerar-relatorio.js",
    "test": "mocha",
    "report": "node scripts/gerar-relatorio.js"
  },
  "devDependencies": {
    "mocha": "^11.0.0"
  },
  "overrides": {
    "diff": "^8.0.3",
    "serialize-javascript": "^7.0.5"
  }
}
